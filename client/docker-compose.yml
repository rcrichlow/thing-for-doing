services:
  client:
    build: .
    ports:
      - "5173:5173"
    volumes:
      - .:/app
      - /app/dist
    environment:
      - CHOKIDAR_USEPOLLING=true
    networks:
      - tfd-network

networks:
  tfd-network:
    name: tfd-network
    external: true
